# components

Reusable UI primitives — domain-agnostic building blocks. Domain-specific components live in `features/`.

| Subdirectory | Contents |
|--------------|----------|
| `ui/` | Generic components: Avatar, Card, Logo, SearchBar, ThemeToggle, modal, toast, stepper, Navbar, Sidebar |
